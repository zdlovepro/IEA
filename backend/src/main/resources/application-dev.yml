spring:
  datasource:
    url: jdbc:mysql://localhost:3306/interactive_edu?useSSL=false&serverTimezone=UTC&characterEncoding=utf8mb4
    username: root
    password: dev123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      password:

minio:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket: courseware
  secure: false

storage:
  type: minio
  local-base-dir: ./data/courseware

python:
  client:
    base-url: http://localhost:8001
    parse-path: /python/v1/parse
    connect-timeout: 5s
    read-timeout: 30s
